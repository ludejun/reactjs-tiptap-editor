export * from './RichTextToolbar';
// `RichTextToolbarButton icon='…'` resolves names from the icon registry; a
// host names a Lucide icon of its own by registering it first.
export { icons, registerIcons, type IconType } from '@/components/icons/icons';
